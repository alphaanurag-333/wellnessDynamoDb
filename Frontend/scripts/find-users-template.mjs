import fs from "fs";
const c = fs.readFileSync("C:/Users/hp/Downloads/IR Wellness Admin Console  (1).html", "utf8");
const m = c.match(/<script type="__bundler\/template"[^>]*>([\s\S]*?)<\/script>/);
const t = JSON.parse(m[1].trim());
const keys = Object.keys(t).filter((k) => /^\d+$/.test(k)).sort((a, b) => Number(a) - Number(b));
const blob = keys.map((k) => t[k]).join("");
const needles = ["{{ userInsightsA }}", "{{ coachTiers }}", "{{ uiGroupA }}", "{{ userInsights }}", "userInsightsA", "uiGroupA"];
for (const n of needles) console.log(n, blob.indexOf(n));
const i = blob.indexOf("{{ coachTiers }}");
if (i >= 0) fs.writeFileSync("src/updatedadmin/_ref-extract-new/users-row-template.txt", blob.slice(i - 2500, i + 5000));
