anRevHero.label }}</div>
                <div style="font-size:10.5px;color:#8ea0bf;margin-top:2px;">{{ anRevHero.scopeLabel }}</div>
                <div style="font-size:31px;font-weight:800;letter-spacing:-.035em;color:#fff;font-variant-numeric:tabular-nums;line-height:1.05;margin-top:12px;">{{ anRevHero.value }}</div>
                <div style="margin-top:auto;padding-top:11px;border-top:1px solid rgba(255,255,255,.12);display:flex;align-items:center;justify-content:space-between;gap:8px;flex-wrap:wrap;">
                  <div>
                    <div style="font-size:9px;color:#8ea0bf;font-weight:700;text-transform:uppercase;letter-spacing:.05em;">{{ anRevHero.monthLabelShort }}</div>
                    <div style="font-size:15px;font-weight:780;color:#fff;font-variant-numeric:tabular-nums;line-height:1.1;">{{ anRevHero.monthValue }}</div>
                  </div>
                  <span style="{{ anRevHero.deltaStyle }}">{{ anRevHero.delta }}</span>
                </div>
              </div>
              <div style="flex:1;min-width:0;display:grid;grid-template-columns:repeat(4,1fr);gap:10px;">
                <sc-for list="{{ anRevCardsAll }}" as="k" hint-placeholder-count="4">
                  <div class="cdact" style="position:relative;overflow:hidden;min-width:0;background:#fff;border:1px solid #eef1f6;border-radius:12px;padding:12px 13px 11px;box-shadow:0 1px 2px rgba(16,35,63,.04);display:flex;flex-direction:column;gap:5px;transition:transform .15s ease,box-shadow .15s ease;" style-hover="box-shadow:0 6px 18px rgba(94,106,210,.12);transform:translateY(-2px);">
                    <span style="{{ k.topBar }}"></span>
                    <div style="font-size:10px;color:#8a97ac;font-weight:700;text-transform:uppercase;letter-spacing:.03em;line-height:1.25;">{{ k.label }}</div>
                    <div style="font-size:19px;font-weight:800;letter-spacing:-.03em;font-variant-numeric:tabular-nums;line-height:1.05;{{ k.accent }}">{{ k.value }}</div>
                    <sc-if value="{{ k.hasShare }}" hint-placeholder-val="{{ true }}">
                      <div style="height:5px;background:#f2f5fa;border-radius:3px;overflow:hidden;margin-top:2px;"><div style="{{ k.barStyle }}"></div></div>
                      <div style="display:flex;align-items:center;justify-content:space-between;gap:6px;"><span style="font-size:9.5px;color:#9aa6b8;font-variant-numeric:tabular-nums;">{{ k.shareLabel }}</span><sc-if value="{{ k.hasDelta }}" hint-placeholder-val="{{ false }}"><span style="{{ k.deltaStyle }}">{{ k.delta }}</span></sc-if></div>
                    </sc-if>
                    <sc-if value="{{ k.isAvg }}" hint-placeholder-val="{{ false }}">
                      <sc-if value="{{ k.hasDelta }}" hint-placeholder-val="{{ false }}"><div style="margin-top:auto;display:flex;align-items:center;gap:6px;"><span style="{{ k.deltaStyle }}">{{ k.delta }}</span><span style="font-size:9.5px;color:#9aa6b8;">vs prev month</span></div></sc-if>
       