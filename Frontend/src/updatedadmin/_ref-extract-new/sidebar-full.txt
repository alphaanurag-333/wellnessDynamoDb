,#34a56a,#7cc79a);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:800;font-size:14.5px;flex:0 0 34px;box-shadow:0 2px 6px rgba(52,165,106,.35);">IR</div>
      <sc-if value="{{ navExpanded }}" hint-placeholder-val="{{ true }}">
        <div style="min-width:0;flex:1;">
          <div style="font-weight:750;font-size:12.5px;letter-spacing:-.01em;color:#16233f;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">India Redefining Wellness</div>
          <div style="font-size:9.5px;letter-spacing:.14em;color:#8a97ac;font-weight:600;margin-top:1px;">ADMIN CONSOLE</div>
        </div>
      </sc-if>
      <button sc-camel-on-click="{{ toggleNavRail }}" title="{{ navToggleTitle }}" style="{{ navToggleStyle }}">{{ navToggleIcon }}</button>
    </div>
    <nav style="{{ navListStyle }}">
      <sc-if value="{{ navExpanded }}" hint-placeholder-val="{{ true }}">
        <div style="font-size:9.5px;font-weight:800;letter-spacing:.09em;text-transform:uppercase;color:#b0bacb;padding:4px 12px 6px;">Sections</div>
      </sc-if>
      <sc-for list="{{ nav }}" as="item" hint-placeholder-count="6">
        <button sc-camel-on-click="{{ item.onClick }}" title="{{ item.label }}" style="{{ item.style }}">
          <span style="{{ item.iconWrap }}">{{ item.icon }}</span>
          <sc-if value="{{ item.showLabel }}" hint-placeholder-val="{{ true }}"><span style="min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">{{ item.label }}</span></sc-if>
        </button>
      </sc-for>
    </nav>
    <div style="{{ navFootStyle }}">
      <div style="position:relative;">
        <button sc-camel-on-click="{{ toggleRoleMenu }}" title="{{ roleMenuTitle }}" style="{{ roleTriggerStyle }}">
          <span style="{{ roleTriggerDot }}"></span>
          <sc-if value="{{ navExpanded }}" hint-placeholder-val="{{ true }}">
            <span style="flex:1;min-width:0;text-align:left;">
              <span style="display:block;font-size:9px;font-weight:800;letter-spacing:.09em;text-transform:uppercase;color:#b0bacb;">Viewing as</span>
              <span style="display:block;font-size:12.5px;font-weight:750;color:#16233f;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ viewAsName }}</span>
            </span>
            <span style="display:flex;color:#9aa6b8;flex:0 0 auto;">{{ roleCaretIcon }}</span>
          </sc-if>
        </button>
        <sc-if value="{{ roleMenuOpen }}" hint-placeholder-val="{{ false }}">
          <div sc-camel-on-click="{{ closeRoleMenu }}" style="position:fixed;inset:0;z-index:55;background:transparent;"></div>
          <div style="{{ roleMenuStyle }}">
            <div style="display:flex;align-items:center;justify-content:space-between;gap:8px;padding:10px 13px 8px;border-bottom:1px solid #f1f4f9;">
              <span style="font-size:9.5px;font-weight:800;letter-spacing:.08em;text-transform:uppercase;color:#8a97ac;">Live roles</span>
              <span style="font-size:10px;color:#b0bacb;font-variant-numeric:tabular-nums;">{{ roleMenuCount }}</span>
            </div>
            <sc-for list="{{ roleSwitchList }}" as="r" hint-placeholder-count="4">
              <button sc-camel-on-click="{{ r.pick }}" style="{{ r.style }}">
                <span style="{{ r.dot }}"></span>
                <span style="flex:1;min-width:0;text-align:left;">
                  <span style="display:block;font-size:12.5px;font-weight:700;color:#16233f;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ r.name }}</span>
                  <span style="display:block;font-size:10.5px;color:#9aa6b8;">{{ r.meta }}</span>
                </span>
                <sc-if value="{{ r.active }}" hint-placeholder-val="{{ false }}"><span style="display:flex;color:#2b8f5b;flex:0 0 auto;">{{ roleTickIcon }}</span></sc-if>
              </button>
            </sc-for>
            <div style="padding:9px 13px;border-top:1px solid #f1f4f9;font-size:10.5px;color:#9aa6b8;text-wrap:pretty;">Switches the whole console to that role's sections and permissions.</div>
          </div>
        </sc-if>
      </div>
      <button title="Logout" style="{{ logoutStyle }}">
        <span style="display:flex;">{{ logoutIcon }}</span>
        <sc-if value="{{ navExpanded }}" hint-placeholder-val="{{ true }}"><span>Logout</span></sc-if>
      </button>
    </div>
  </aside>

  <div style="flex:1;display:flex;flex-direction:column;min-width:0;height:100vh;">
    <header style="height:62px;flex:0 0 62px;background:rgba(255,255,255,.85);backdrop-filter:blur(12px);border-bottom:1px solid #e6ebf2;display:flex;align-items:center;gap:12px;padding:0 16px;overflow-x:auto;z-index:20;">
      <sc-if value="{{ navCanBack }}" hint-placeholder-val="{{ false }}">
        <button sc-camel-on-click="{{ na