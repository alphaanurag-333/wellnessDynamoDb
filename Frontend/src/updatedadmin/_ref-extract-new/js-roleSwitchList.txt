olor:#8a97ac;">Live roles</span>
              <span style="font-size:10px;color:#b0bacb;font-variant-numeric:tabular-nums;">{{ roleMenuCount }}</span>
            </div>
            <sc-for list="{{ roleSwitchList }}" as="r" hint-placeholder-count="4">
              <button sc-camel-on-click="{{ r.pick }}" style="{{ r.style }}">
                <span style="{{ r.dot }}"></span>
                <span style="flex:1;min-width:0;text-align:left;">
                  <span style="display:block;font-size:12.5px;font-weight:700;color:#16233f;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ r.name }}</span>
                  <span style="display:block;font-size:10.5px;color:#9aa6b8;">{{ r.meta }}</span>
                </span>
                <sc-if value="{{ r.active }}" hint-placeholder-val="{{ false }}"><span style="display:flex;color:#2b8f5b;flex:0 0 auto;">{{ roleTickIcon }}</span></sc-if>
              </button>
            </sc-for>
            <div style="padding:9px 13px;border-top:1px solid #f1f4f9;font-size:10.5px;color:#9aa6b8;text-wrap:pretty;">Switches the whole console to that role's sections and permissions.</div>
          </div>
        </sc-if>
      </div>
      <button title="Logout" style="{{ logoutStyle }}">
        <span style="display:flex;">{{ logoutIcon }}</span>
        <sc-if value="{{ navExpanded }}" hint-placeholder-val="{{ true }}"><span>Logout</span></sc-if>
      </button>
    </div>
  </aside>

  <div style="flex:1;display:flex;flex-direction:column;min-width:0;height:100vh;">
    <header style="height:62px;flex:0 0 62px;background:rgba(255,255,255,.85);backdrop-filter:blur(12px);border-bottom:1px solid #e6ebf2;display:flex;align-items:center;gap:12px;padding:0 16px;overflow-x:auto;z-index:20;">
      <sc-if value="{{ navCanBack }}" hint-placeholder-val="{{ false }}">
        <button sc-camel-on-click="{{ navBack }}" title="{{ navBackTitle }}" style="flex:0 0 auto;height:34px;padding:0 12px 0 9px;display:flex;align-items:cent