Nutritions'],['rate','Health Progress'],['reflection','Daily Reflection form'],['prescription','Wellness Prescription'],['presentable','Presentable Pics'],['exchange','Energy Exchange'],['protocol','Protocol Settings']];\n      const menuDef = isHeal?menuAll : isConsult?[['profile','Personal Details'],['consult','Consultation']] : [['profile','Personal Details']];\n      const curTab = menuDef.map(m=>m[0]).includes(s.drawerTab)?s.drawerTab:'profile';\n      // Presentable pics — coach requests a client photo; approve/reject is one-time\n      const ppEnabled=s.ppEnabled!==false; const ppCanEdit=this.editable();\n      const ppDec=s.ppDecision||{pp2:'approved'};\n      const ppDefs=[{id:'pp1',date:'22 Jul 2026',note:'Front pose · gym'},{id:'pp2',date:'08 Jul 2026',note:'Portrait'},{id:'pp3',date:'25 Jun 2026',note:'Full body'}];\n      const ppMeta={approved:{lbl:'Approved',fg:'#1f7a4d',bg:'#e7f6ee',bd:'#cdeede'},rejected:{lbl:'Rejected',fg:'#c0392b',bg:'#fdeaea',bd:'#f6d0d1'},pending:{lbl:'Pending review',fg:'#9a7a00',bg:'#fdf6e3',bd:'#f2e2a8'}};\n      const ppPics=ppDefs.map(p=>{const st=ppDec[p.id]||'pending';const m=ppMeta[st];const decided=st!=='pending';\n        return {id:p.id,date:p.date,note:p.note,decided,pending:st==='pending',approved:st==='approved',\n          slotId:'pp-'+du.name.replace(/[^a-zA-Z0-9]/g,'')+'-'+p.id,\n          statusLabel:m.lbl,statusStyle:{fontSize:'10.5px',fontWeight:700,letterSpacing:'.03em',textTransform:'uppercase',color:m.fg,background:m.bg,border:'1px solid '+m.bd,borderRadius:'6px',padding:'3px 9px',whiteSpace:'nowrap'},\n          approve:()=>{if(ppDec[p.id]||!ppCanEdit)return;this.setState(s2=>({ppDecision:{...(s2.ppDecision||{pp2:'approved'}),[p.id]:'approved'}}));this.emit('Photo approved');},\n          reject:()=>{if(ppDec[p.id]||!ppCanEdit)return;this.setState(s2=>({ppDecision:{...(s2.ppDecision||{pp2:'approved'}),[p.id]:'rejected'}}));this.emit('Photo rejected');},\n          download:()=>this.emit('Downloading photo — '+p.note)};});\n      const ppPendingCount=ppPics.filter(p=>p.pending).length;\n      const ppToggle=()=>{if(ppCanEdit)this.setState({ppEnabled:!(s.ppEnabled!==false)});};\n      const ppRequest=()=>this.emit('Request sent — '+du.name.split(' ')[0]+' notified to upload a presentable photo');\n      const ppToggleStyle={position:'relative',width:'42px',height:'24px',flex:'0 0 42px',borderRadius:'12px',border:'none',cursor:ppCanEdit?'pointer':'default',background:ppEnabled?'#2b8f5b':'#cdd5e0',transition:'background .15s ease'};\n      const ppKnobStyle={position:'absolute',top:'3px',left:ppEnabled?'21px':'3px',width:'18px',height:'18px',borderRadius:'50%',background:'#fff',transition:'left .15s ease',boxShadow:'0 1px 2px rgba(0,0,0,.2)'};\n      const drawerMenu = menuDef.map(m=>{const active=curTab===m[0];const soon=!built.includes(m[0]);return {id:m[0],label:m[1],soon,caret:this.icon(I.chev,15),onClick:()=>this.setState({drawerTab:m[0]}),style:{display:'flex',alignItems:'center',width:'100%',padding:'11px 12px',border:'none',borderRadius:'9px',fontSize:'13px',fontWeight:active?700:600,cursor:'pointer',textAlign:'left',color:active?'#16233f':'#2f6fd6',background:active?'#eef4ff':'transparent',transition:'all .12s'}};});\n      const bUnit=s.bodyUnit||'cm'; const bPer=s.bodyPeriod||'month';\n      const monthLbls=['Jul','Jun','May','Apr','Mar','Feb','Jan']; const weekLbls=['15 Jul','08 Jul','01 Jul','24 Jun','17 Jun','10 Jun','03 Jun','27 May'];\n      const pLbls = bPer==='week'?weekLbls:monthLbls;\n      const target = Math.min(Math.max(s.bodyEnd||0,0), pLbls.length-1);\n      const pStart = Math.min(Math.max(target-2,0), Math.max(pLbls.length-3,0));\n      const idxs=[]; for(let k=pStart;k<Math.min(pStart+3,pLbls.length);k++) idxs.push(k);\n      const stepF = bPer==='week'?0.25:1;\n      const bodyDates = idxs.map(i=>pLbls[i]);\n      const bodyEndOptions = pLbls.map((l,i)=>({i:i,label:l}));\n      const conv=(v)=>bUnit==='inch'?(v/2.54):v; const uSuf=bUnit==='inch'?' in':' cm';\n      const measDef=[['Neck',38,0.5],['Shoulder',112,-0.5],['Chest',96,2],['Waist',82,4],['Hip',98,2],['Thighs',56,1]];\n      const bodyMeasures = measDef.map(m=>{const vals=idxs.map(i=>conv(m[1]+i*m[2]*stepF));const d=vals.length>1?+(vals[0]-vals[1]).toFixed(1):0;return {name:m[0],vals:vals.map(x=>x.toFixed(1)),delta:(d>0?'+':'')+d+uSuf,deltaStyle:this.badge(d<0?'#e7f6ee':d>0?'#fdecec':'#f2f5fa',d<0?'#2b8f5b':d>0?'#d64545':'#8a97ac')};});\n      const metaDef=[['BMI',27.4,0.7,''],['BMR',1420,-15,' kcal'],['TDEE',2050,-30,' kcal'],['Body fat %',31.2,1.6,'%'],['Lean muscle %',27.8,-0.85,'%'],['Visceral fat',9,1,''],['Fatty liver idx',1.8,0.3,'']];\n      const metabolic = metaDef.map(m=>{const raw=idxs.map(i=>+(m[1]+i*m[2]*stepF).toFixed(1));const d=raw.length>1?+(raw[0]-raw[1]).toFixed(1):0;const good=(m[0]==='Lean muscle %')?d>0:d<0;return {name:m[0],vals:raw.map(x=>x+m[3]),delta:(d>0?'+':'')+d,deltaStyle:this.badge(good?'#e7f6ee':'#fdecec',good?'#2b8f5b':'#d64545')};});\n      const bodyPhotoSets = idxs.map(i=>({date:pLbls[i]+' 2026'}));\n      const bodyWeeklyNote = bPer==='week';\n      const testCatalog=['CBC','HbA1c','Lipid Profile','Thyroid (TSH, T3, T4)','Vitamin D','Vitamin B12','Liver Function (LFT)','Kidney Function (KFT)','Iron Studies','Fasting Blood Sugar'];\n      const recTests=(s.recTests&&s.recTests.length)?s.recTests:['CBC','HbA1c','Lipid Profile'];\n      const internalTests=recTests.map(t=>({name:t,remove:()=>this.setState(st=>({recTests:((st.recTests&&st.recTests.length)?st.recTests:['CBC','HbA1c','Lipid Profile']).filter(x=>x!==t)}))}));\n      const internalAddOptions=testCatalog.filter(t=>!recTests.includes(t));\n      const aiDates=['14 Dec 25','2 Mar 26','4 May 26','5 May 26'];\n      const aiData=[\n        ['Glucose Panel'],\n        ['HbA1c','5 – 5.3%','Below 5.7%',['Consistently in the diabetic range and rising over the last three draws. This reflects a sustained glucose load over ~3 months and points to worsening 