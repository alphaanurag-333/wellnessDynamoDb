Personal details updated for '+du.name);},\n        drawerActivity:[['Logged a coaching session','2h ago'],['Updated health goals','yesterday'],['Completed Prakruti assessment','3 days ago'],['Joined program: '+du.goal,du.joined]].map(a=>({title:a[0],time:a[1]})),\n        drawerNotes:[{text:'Client responding well to the '+du.goal.toLowerCase()+' plan. Energy levels improving.',author:du.coach,time:'2 days ago'},{text:'Requested a diet chart revision — follow up next session.',author:du.coach,time:'1 week ago'}],\n        healthHidden:hidePii, drawerMenu, planActive:((isHeal||isConsult)&&du.status==='Active'&&du.daysLeft>0), planDaysLeft:du.daysLeft,\n        mIsProfile:(curTab==='profile'), mIsDashboard:(curTab==='dashboard'), mIsPlaceholder:(!built.includes(curTab)), placeholderTitle:((menuDef.find(m=>m[0]===curTab)||[])[1]||''),\n        showLocked:(hidePii && ['body','internal','launch','tracking','nutrition','consult'].includes(curTab)),\n        showIpTests:(s.ipTab||'tests')==='tests', showIpAnalysis:(s.ipTab||'tests')==='analysis',\n        setIpTests:()=>this.setState({ipTab:'tests'}), setIpAnalysis:()=>this.setState({ipTab:'analysis'}),\n        ipTestsStyle:this.segStyle((s.ipTab||'tests')==='tests'), ipAnalysisStyle:this.segStyle((s.ipTab||'tests')==='analysis'),\n        launchSections, launchTotal, launchMax, lifeScore, launchEditable:this.editable(), launchReadOnly:!this.editable(),\n        ...(()=>{const info=s.launchInfo;\n          const lbands=(s.cfgLaunchBands||this.LAUNCH_BAND_SEED).slice().sort((a,b)=>b.pts-a.pts);\n          const qCfg=(()=>{if(!info)return null;const secs=s.cfgLaunchSecs||[];for(let i=0;i<secs.length;i++){const hit=secs[i].qs.find(x=>x.q===info.q);if(hit)return hit;}return null;})();\n          const qNote=(lbl)=>((qCfg&&qCfg.info&&qCfg.info[lbl])||'').trim();\n          const guess=(a)=>{const x=(a||'').toLowerCase();\n            const pick=(p)=>lbands.reduce((best,b)=>Math.abs(b.pts-p)<Math.abs(best.pts-p)?b:best,lbands[0]);\n            if(/never|very rarely|excellent|daily|always/.test(x))return pick(90);\n            if(/rarely|often|good|mostly/.test(x))return pick(75);\n            if(/sometimes|occasionally|moderate|average/.test(x))return pick(55);\n            if(/frequently|poor|seldom|hardly/.test(x))return pick(30);\n            return pick(60);};\n          return {\n            launchInfoOpen:!!info,\n            launchInfoQ:info?info.q:'',\n            launchInfoAnswer:info?(info.a||'\\u2014'):'',\n            launchInfoSuggested:info?(guess(info.a).label+' \\u00b7 '+guess(info.a).pts):'',\n            launchInfoBands:lbands.map(b=>{const t=this.bandTone(b.pts);\n              return {range:b.label,text:qNote(b.label)||b.note||'\\u2014',\n              rowStyle:{display:'flex',alignItems:'flex-start',gap:'10px',background:t.bg,border:'1px solid '+t.bd,borderRadius:'10px',padding:'9px 11px'},\n              rangeStyle:{flex:'0 0 82px',fontSize:'11px',fontWeight:750,color:t.fg,textTransform:'uppercase',letterSpacing:'.03em',whiteSpace:'nowrap'},\n              ptsStyle:{flex:'0 0 auto',fontSize:'11px',fontWeight:750,color:t.fg,fontVariantNumeric:'tabular-nums'},\n              pts:b.pts};}),\n            launchInfoTip:(qCfg&&(qCfg.tip||'').trim())||'Rate what the client actually reported, not intent. If the reply is vague, ask in the next call and leave the rating where it is \\u2014 the admin-set points below decide the score.',\n            launchInfoStop:(e)=>e.stopPropagation(),\n            closeLaunchInfo:()=>this.setState({launchInfo:null}),\n            applyLaunchSuggested:()=>{if(!info||!this.editable())return;\n              const b=guess(info.a); const v=Math.round(b.pts/100*(info.weight||100));\n              this.setState(st=>{const ls={...(st.launchScores||{})};ls[info.si]={...(ls[info.si]||{}),[info.qi]:v};return {launchScores:ls,launchInfo:null};});\n              this.emit('Rated '+b.label+' \\u00b7 '+v+' / '+info.weight);}};})(),\n        launchMode, showLifestyle:launchMode==='lifestyle', showPrakriti:launchMode==='prakriti',\n        setLifestyle:()=>this.setState({launchMode:'lifestyle'}), setPrakriti:()=>this.setState({launchMode:'prakriti'}),\n        lifestyleSegStyle:this.segStyle(launchMode==='lifestyle'), prakritiSegStyle:this.segStyle(launchMode==='prakriti'),\n        doshaGroups, prakritiType, prakritiElement, doshaBars,\n        asmtCanRerun, asmtLocked:!asmtCanRerun, asmtRoleName,\n        lifeHist, lifeHasHist:lifeHist.length>0, lifeAttempt:'Attempt '+(lifeHist.length+1),\n        prakHist, prakHasHist:prakHist.length>0, prakAttempt:'Attempt '+(prakHist.length+1),\n        lifeHistOpen:!!((s.asmtOpen||{}).lifestyle), prakHistOpen:!!((s.asmtOpen||{}).prakriti),\n        toggleLifeHist:()=>this.setState(st=>({asmtOpen:{...(st.asmtOpen||{}),lifestyle:!((st.asmtOpen||{}).lifestyle)}})),\n        togglePrakHist:()=>this.setState(st=>({asmtOpen:{...(st.asmtOpen||{}),prakriti:!((st.asmtOpen||{}).prakriti)}})),\n        lifeHistLabel:(((s.asmtOpen||{}).lifestyle)?'Hide history':'History')+' \\u00b7 '+lifeHist.length,\n        prakHistLabel:(((s.asmtOpen||{}).prakriti)?'Hide history':'History')+' \\u00b7 '+prakHist.length,\n        rerunLife:asmtArchive('lifestyle'), rerunPrak:asmtArchive('prakriti'),\n        goLaunchLifestyle:()=>this.setState({drawerTab:'launch',launchMode:'lifestyle'}),\n        goLaunchPrakriti:()=>this.setState({drawerTab:'launch',launchMode:'prakriti'}),\n        prakRecItems:prakRecCard.items, addPrakRec:prakRecCard.add,\n        prakAvoidItems:prakAvoidCard.items, addPrakAvoid:prakAvoidCard.add,\n        prakEditAttr, prakEditable,\n        expandAllLaunch:()=>this.setState({launchCollapse:{}}),\n        collapseAllLaunch:()=>this.setState({launchCollapse:{0:true,1:true,2:true,3:true,4:true}}),\n        focusItems, focusEmpty:focusItems.length===0, addFocus, focusInput:s.focusInput||'',\n        onFocusInput:(e)=>this.setState({focusInput:e.target.value}),\n        onFocusKey:(e)=