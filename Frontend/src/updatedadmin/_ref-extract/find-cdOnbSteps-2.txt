cdOnbSteps.forEach((stp,i)=>{const kind=onbActs[i]; if(!kind)return;\n        const doneNote=onbDoneBy[i];\n        if(kind==='rca'){\n          stp.hasAction=onbCanAct&&!stp.done;\n          stp.actionLabel='Submit RCA';\n          stp.actionStyle=actBtn(true);\n          stp.action=()=>markStep(i,stp.label,'RCA submitted by '+(s.viewAs==='admin'?'Admin desk':du.coach)+' \\u00b7 '+onbStamp);\n          stp.hasNote=!!doneNote; stp.note=doneNote||'';\n          return;}\n        if(kind==='brief'||kind==='hap'||kind==='initiate'){\n          const mk=kind==='brief'?'Reports briefing':(kind==='hap'?'HAP session':'Program initiation');\n          const bm=((s.meetings||[]).filter(m=>m.client===du.name&&m.kind===mk))[0];\n          stp.hasAction=onbCanAct;\n          stp.actionLabel=bm?'Reschedule':(kind==='brief'?'Schedule briefing':(kind==='hap'?'Schedule HAP':'Schedule initiation'));\n          stp.actionStyle=actBtn(!bm);\n          stp.action=()=>this.setState({lmFormOpen:true,lmClient:du.name,lmKind:mk,\n            lmForm:{date:'',dur:45,mode:'Video call',\n              note:kind==='brief'?'We will walk through your reports together.':(kind==='hap'?'Health Action Plan session \\u2014 we will set your plan together.':'Program initiation call \\u2014 your journey starts here.'),\n              picks:[],ttl:24}});\n          stp.hasNote=!!bm||!!doneNote;\n          stp.note=bm?(bm.status==='confirmed'\n              ? ('Booked '+bm.pick.date+' at '+bm.pick.time)\n              : (bm.offers.length+' slot'+(bm.offers.length===1?'':'s')+' offered \\u00b7 awaiting '+du.name.split(' ')[0]))\n            :(doneNote||'');\n          return;}\n        const labels={\n          letter:['Upload letter','Commitment letter uploaded'],initiate:['Initiate program','Program initiated']};\n        stp.hasAction=onbCanAct&&!stp.done;\n        stp.actionLabel=labels[kind][0];\n        stp.actionStyle=actBtn(false);\n        stp.action=()=>markStep(i,stp.label,labels[kind][1]+' by '+(s.viewAs==='admin'?'Admin desk':du.coach)+' \\u00b7 '+onbStamp);\n        stp.hasNote=!!doneNote; stp.note=doneNote||'';});\n      const onbTab={0:'profile',1:'profile',2:'body',3:'internal',4:'launch',8:'protocol'};\n      cdOnbSteps.forEach((stp,i)=>{if(stp.hasAction||!onbCanAct||i===9)return;\n        const tab=onbTab[i];\n        if(tab&&!stp.done){\n          stp.hasAction=true; stp.actionLabel='Open \\u203a'; stp.actionStyle=actBtn(false);\n          stp.action=()=>this.setState({drawerTab:tab});\n          return;}\n        if(stp.done){stp.hasAction=true;stp.actionLabel='Undo';stp.actionStyle={height:'26px',padding:'0 10px',borderRadius:'8px',flex:'0 0 auto',fontFamily:'inherit',fontSize:'10.5px',fontWeight:700,cursor:'pointer',whiteSpace:'nowrap',border:'1px solid #eef1f6',background:'#fff',color:'#b6c1d1'};stp.action=stp.toggle;return;}\n        stp.hasAction=true; stp.actionLabel='Mark done'; stp.actionStyle=actBtn(false); stp.action=stp.toggle;});\n      const cdOnbDoneCount=cdOnbSteps.filter(x=>x.done).length;\n      const cdOnbPct=Math.round(cdOnbDoneCount/cdOnbDefs.length*100);\n      const cdOnbDoneReal=cdOnbDoneCount===cdOnbDefs.length;\n      const cdOnbView=s.cdOnbView||'auto'; // auto | pre | post — demo override to preview both states\n      const cdOnbDone=cdOnbView==='post'?true:cdOnbView==='pre'?false:cdOnbDoneReal;\n      const cdOnbStatus=cdOnbDone?'Completed':('In progress · '+cdOnbDoneCount+'/'+cdOnbDefs.length+' steps');\n      // Champion race scores — daily score comes from the DRF page\n      const cdDrfVals=Object.values(s.drfScores||{}); const cdDaily=cdDrfVals.length?Math.min(100,Math.round(cdDrfVals.reduce((a,b)=>a+(+b||0),0)/cdDrfVals.length*10)):91;\n      const cdMonthly=240+(cdSeed%60);\n      const cdPos=(du.name==='Madhupriya Bilas')?1:(1+(cdSeed%8)); const cdPosOrd=cdPos+(['th','st','nd','rd'][(cdPos%100>>3^1&&cdPos%10)]||'th');\n      const cdIsChamp=cdPos===1;\n      const cdChampTotal=24;\n      const cdDailyRing={background:'conic-gradient(#5e6ad2 '+(cdDaily*3.6).toFixed(0)+'deg,#e9ecf5 0)',width:'62px',height:'62px',borderRadius:'50%',flex:'0 0 62px',display:'flex',alignItems:'center',justifyContent:'center'};\n      const cdMonthlyDelta='Rank '+cdPosOrd+' of '+cdChampTotal;\n      const cdPosColor=cdPos===1?'#b8860b':cdPos<=3?'#5a6b85':'#8a97ac';\n      const cdPosBadge={width:'52px',height:'52px',flex:'0 0 52px',borderRadius:'50%',background:cdPos===1?'linear-gradient(135deg,#f5d67a,#d4af37)':'#eef1f6',color:cdPos===1?'#7a5c00':'#5a6b85',display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',fontWeight:780,border:cdPos===1?'none':'1px solid #e0e5ee'};\n      const cdScoreCards=[\n        ['Daily score',cdDaily+' / 100 · from DRF','#5e6ad2'],\n        ['Monthly score',String(cdMonthly),'#0d9488'],\n        ['Champion race',cdPosOrd+' place','#b8860b']\n      ].map(k=>({label:k[0],val:k[1],accent:{color:k[2]}}));\n      // Message to client\n      const cdMsg=s.cdMsg||'';\n      const onCdMsg=(e)=>this.setState({cdMsg:e.target.value});\n      const sendCdMsg=()=>{const v=(s.cdMsg||'').trim();if(!v)return;this.splashMsg();this.setState({cdMsg:''});clearTimeout(this._rt);this._rt=setTimeout(()=>this.emit('Message delivered to '+du.name.split(' ')[0]+\"'s app\"),1700);};\n      const cdQuickMsg=()=>{this.splashMsg();clearTimeout(this._rt);this._rt=setTimeout(()=>this.emit('Message delivered to '+du.name.split(' ')[0]+\"'s app\"),1700);};\n\n      // ---- Coach-set targets (water / sleep) — latest value surfaces in At a Glance ----\n      const tgtCanEdit=['admin','wc'].includes(s.viewAs);\n      const tgtRole=s.viewAs==='admin'?'Admin':'Wellness Coach';\n      const tgtBy=s.viewAs==='admin'?'Admin desk':du.coach;\n      const tgtKey=(k)=>du.name+'|'+k;\n      const tgtSeed={water:8,sleep:8};\n      const tgtVal=(k)=>{const t=(s.tgt||{})[tgtKey(k)];return t!=null?t:tgtSeed[k];};\n      const tgtDraftOf=(k)=>{const d=(s.tgtDraft||{})[tgtKey(k)];return d!=nu