Personal details',rows:[\n              {k:'Full name',v:so.name},{k:'Email',v:so.email},\n              {k:'Mobile',v:'+91 98'+String(so.name.length*7919%100000000).padStart(8,'0').slice(0,8)},\n              {k:'Date of birth',v:['14 Feb 1990','23 Jul 1988','05 Nov 1993','18 Apr 1991'][so.name.length%4]},\n              {k:'Location',v:['Pune','Mumbai','Nashik','Nagpur'][so.name.length%4]}\n            ]},\n            {name:'Role & engagement',rows:[\n              {k:'Role',v:so.role},{k:'Status',v:so.status},{k:'Detail',v:so.meta},\n              {k:'Joined',v:['12 Jan 2025','04 Mar 2025','19 Aug 2024','02 Feb 2026'][so.name.length%4]},\n              {k:'Referral code',v:'IRW-'+so.roleKey.toUpperCase()+'-'+String(so.name.length*37%900+100)}\n            ]}\n          ]:[],\n          ...(()=>{if(!so)return {promoVisible:false,promoPendingHas:false};\n            const rank={trainee:1,support:2,awc:3,wc:4};\n            const isAdm=s.viewAs==='admin';\n            const canReq=s.viewAs==='wc'&&soRaw.roleKey==='awc';\n            const reqs=s.promoReqs||[];\n            const pend=reqs.find(r=>r.member===so.name&&r.status==='pending')||null;\n            const opts=['wc','awc','support','trainee'].filter(k=>k!==so.roleKey).map(k=>({id:k,label:roleNm[k]}));\n            const tgt=(s.promoPick&&opts.some(o=>o.id===s.promoPick))?s.promoPick:(opts[0]&&opts[0].id);\n            const up=(rank[tgt]||0)>(rank[so.roleKey]||0);\n            const verb=up?'Promote to ':'Move to ';\n            const apply=(k)=>this.setState(st=>({staffRoleOv:{...(st.staffRoleOv||{}),[so.name]:k},staffOpen:{...st.staffOpen,roleKey:k,role:roleNm[k]},promoPick:null}));\n            return {\n              promoVisible:(isAdm||canReq)&&!pend,\n              promoNote:isAdm?('Admin · applies at once. Current role: '+so.role):('You can request a change for your assistant coach — Admin approves it. Current role: '+so.role),\n              promoOpts:opts, promoTarget:tgt||'',\n              onPromoTarget:(e)=>this.setState({promoPick:e.target.value}),\n              promoBtnTitle:(isAdm?verb:'Send for approval · '+verb.toLowerCase())+(roleNm[tgt]||''),\n              promoBtnStyle:{height:'34px',padding:'0 16px',border:'none',background:isAdm?'#5e6ad2':'#2b8f5b',color:'#fff',fontWeight:650,fontSize:'12.5px',cursor:'pointer',fontFamily:'inherit',whiteSpace:'nowrap'},\n              promoAct:()=>{if(!tgt)return;\n                if(isAdm){apply(tgt);this.emit(so.name+' is now '+roleNm[tgt]);return;}\n                this.setState(st=>({promoReqs:[{id:Date.now()+'-'+so.name,member:so.name,from:so.roleKey,to:tgt,by:'Anita Rao (Wellness Coach)',when:'04 Aug 2026',status:'pending'},...(st.promoReqs||[])],promoPick:null}));\n                this.emit('Sent to Admin for approval · '+so.name+' → '+roleNm[tgt]);},\n              promoPendingHas:!!pend, promoIsAdmin:isAdm, promoIsRequester:!isAdm,\n              promoPendingTitle:pend?(so.name+' → '+roleNm[pend.to]+' · awaiting Admin approval'):'',\n              promoPendingMeta:pend?('Requested by '+pend.by+' · '+pend.when+' · from '+roleNm[pend.from]):'',\n              promoApprove:()=>{if(!pend)return;apply(pend.to);\n                this.setState(st=>({promoReqs:(st.promoReqs||[]).map(r=>r.id===pend.id?{...r,status:'approved'}:r)}));\n                this.emit('Approved · '+so.name+' is now '+roleNm[pend.to]);},\n              promoReject:()=>{if(!pend)return;this.setState(st=>({promoReqs:(st.promoReqs||[]).map(r=>r.id===pend.id?{...r,status:'rejected'}:r)}));\n                this.emit('Rejected · '+so.name+' stays '+roleNm[pend.from]);},\n              promoWithdraw:()=>{if(!pend)return;this.setState(st=>({promoReqs:(st.promoReqs||[]).filter(r=>r.id!==pend.id)}));\n                this.emit('Request withdrawn for '+so.name);}};})(),\n          closeStaffPage:()=>this.setState({staffOpen:null})};\n      })(),\n      // flags\n      flags,\n      // configs\n      ...(()=>{const cur=s.configTab||'app';\n        const defs=[['app','App','📱','app-only keys — onboarding steps, push notifications, in-app content slots'],['web','Web','🌐','web-only keys — landing sections, SEO metadata, marketing banners'],['common','Common','🔗','shared keys used by both surfaces — branding, plans, legal copy'],['features','Feature flags','🚩','feature flags — rollout state per surface']];\n        const d=defs.find(x=>x[0]===cur)||defs[0];\n        return {cfgTabs:defs.map(x=>({label:x[1],style:this.segStyle(cur===x[0]),pick:()=>this.setState({configTab:x[0]})})),\n          cfgIsFeatures:cur==='features', cfgNotFeatures:cur!=='features',\n          cfgIcon:d[2], cfgScopeLabel:d[1].toLowerCase(), cfgScopeHint:'Tell me which groups and keys to add here — '+d[3]+'.'};})(),\n      ...(()=>{\n        const scope=s.configTab||'common';\n        const role=s.viewAs;\n        const roleEditable={onb_vid:['wc']};\n        const ovGalVids=[['IRW default welcome','4:00'],['Coach intro \\u00b7 Anita Rao','4:10'],['Coach intro \\u00b7 Priya Nair','3:48'],['Journey kickoff \\u00b7 generic','3:30'],['How the app works','2:55'],['Week one walkthrough','5:10']];\n        const ovGalSeedTag={'IRW default welcome':'ONB-DEFAULT','Coach intro \\u00b7 Anita Rao':'ONB-WC01','Coach intro \\u00b7 Priya Nair':'ONB-WC02'};\n        const ovTagOf=(name)=>{const pk=s.ovPick||{};const hit=Object.keys(pk).filter(c=>pk[c]===name);\n          if(hit.length)return hit.join(', ');\n          const seedT=ovGalSeedTag[name];\n          return (seedT&&!pk[seedT])?seedT:'';};\n        const canEdit=(role==='admin'||role==='support')||((roleEditable[s.cfgOpen]||[]).indexOf(role)>-1);\n        const roDim=canEdit?{}:{opacity:.55,pointerEvents:'none'};\n        const gate=(base)=>canEdit?base:Object.assign({},base,{opacity:.55,pointerEvents:'none'});\n        const defs=[\n          {id:'banner',group:'Banners & cards',name:'Banner',app:1,web:1,kind:'banner',note:'Multiple placements \\u00b7 aspect ratio adapts per type',owner:'Adm