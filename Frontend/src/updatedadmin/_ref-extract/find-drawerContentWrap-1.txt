drawerContentWrap:s.drawerMenuOpen===false?'max-width:1080px;margin:0 auto;padding:26px 44px 60px;':'max-width:820px;margin:0 auto;padding:26px 28px 60px;',\n      dtProfile:()=>this.setState({drawerTab:'profile'}), dtActivity:()=>this.setState({drawerTab:'activity'}), dtNotes:()=>this.setState({drawerTab:'notes'}),\n      dtHealth:()=>this.setState({drawerTab:'health'}), dtTracking:()=>this.setState({drawerTab:'tracking'}), dtConsult:()=>this.setState({drawerTab:'consult'}),\n      dtProfileBtn:this.tabStyle(s.drawerTab==='profile'), dtActivityBtn:this.tabStyle(s.drawerTab==='activity'), dtNotesBtn:this.tabStyle(s.drawerTab==='notes'),\n      dtHealthBtn:this.tabStyle(s.drawerTab==='health'), dtTrackingBtn:this.tabStyle(s.drawerTab==='tracking'), dtConsultBtn:this.tabStyle(s.drawerTab==='consult'),\n      dtIsProfile:s.drawerTab==='profile', dtIsActivity:s.drawerTab==='activity', dtIsNotes:s.drawerTab==='notes',\n      dtIsHealth:s.drawerTab==='health', dtIsTracking:s.drawerTab==='tracking', dtIsConsult:s.drawerTab==='consult',\n      ...drawer,\n      // modal\n      modalOpen:!!s.modal, closeModal:()=>this.setState({modal:null}),\n      modalIsAttach:s.modal==='attach', modalIsForm:s.modal==='role'||s.modal==='policy',\n      modalTitle:s.modal==='attach'?'Attach policy':s.modal==='role'?'Create role':'Create policy',\n      modalDesc:s.modal==='attach'?'Grant this policy to a whole role or a single user. Denies always win over allows.':s.modal==='role'?'Roles are baseline permission bundles. You can refine them in the matrix.':'Policies are reusable allow/deny grants you can attach anywhere.',\n      modalPlaceholder:s.modal==='role'?'Role name':'Policy name', modalCta:s.modal==='attach'?'Attach':'Create',\n      attachRole:()=>this.setState({attachTarget:'role'}), attachUser:()=>this.setState({attachTarget:'user'}),\n      attachRoleBtn:{flex:1,height:'40px',borderRadius:'9px',border:'1px solid '+(s.attachTarget==='role'?'#34a56a':'#e6ebf2'),background:s.attachTarget==='role'?'#f2fbf6':'#fff',fontSize:'13px',fontWeight:600,color:s.attachTarget==='role'?'#2b8f5b':'#5a6b85',cursor:'pointer'},\n      attachUserBtn:{flex:1,height:'40px',borderRadius:'9px',border:'1px solid '+(s.attachTarget==='user'?'#34a56a':'#e6ebf2'),background:s.attachTarget==='user'?'#f2fbf6':'#fff',fontSize:'13px',fontWeight:600,color:s.attachTarget==='user'?'#2b8f5b':'#5a6b85',cursor:'pointer'},\n      attachOptions:s.attachTarget==='role'?['Wellness Coach','Asst. Coach','Support']:['Madhupriya Bilas','Priya Nair','Vikram Sethi'],\n      confirmModal:()=>{ const m=s.modal; this.setState({modal:null}); this.emit(m==='attach'?'Policy attached':m==='role'?'Role created':'Policy created'); },\n      // toast\n      toastOpen:!!s.toast, toastMsg:s.toast, bellSplashOn:!!s.bellSplash, msgSplashOn:!!s.msgSplash\n    };\n  }\n}\n<\u002Fscript>\n\n\n<\u002Fbody><\u002Fhtml>"
  </script>
</body>
</html>